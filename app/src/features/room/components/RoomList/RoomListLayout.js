import React, { Component } from 'react';

export class RoomListLayout  extends Component {
  render() {
    let { rooms, activeRoom, isShowing} = this.props;
    return (
      <div className="room-list">
      </div>
    );
  };
}